import React from 'react'

export default function ParteTeorica() {
  return (
    <section>
      <h2>Parte teórica - Resumen</h2>
      <ol>
        <li>DOM: Document Object Model — representación en árbol del documento HTML.</li>
        <li>JSX: Sintaxis que permite mezclar XML/HTML con JavaScript en React.</li>
        <li>Estado y props: props = datos de entrada (inmutables), estado = datos internos (mutables con setState).</li>
      </ol>
    </section>
  )
}
